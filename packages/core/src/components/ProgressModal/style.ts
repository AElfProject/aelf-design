import { createStyles } from 'antd-style'

export default createStyles(({ css }) => {
  return {
    progressModalContainer: css`
      width: 680px !important;
    `,
    progressBarContainer: css`
      margin-top: 32px;
    `
  }
})
