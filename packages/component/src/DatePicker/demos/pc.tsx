import React from 'react';
import { DatePickerForPC } from 'aelf-design';

export default function DatePickerForPCExample() {
  return (
    <div>
      <DatePickerForPC showTime={{ format: 'HH:mm' }} />
    </div>
  );
}
