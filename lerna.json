{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "0.1.58-alpha.0",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "commands": {
    "build": {
      "ignore": [
        "*.test.js"
      ],
      "npmClient": "npm",
      "script": "build",
      "stream": true
    }
  },
  "scripts": {
    "preversion": "lerna run build"
  }
}
