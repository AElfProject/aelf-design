{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "0.1.64-alpha.0",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "commands": {
    "build": {
      "ignore": [
        "*.test.js"
      ],
      "npmClient": "yarn",
      "script": "build",
      "stream": true
    }
  },
  "preversion": "lerna run build",
  "scripts": {
    "preversion": "lerna run build"
  }
}
